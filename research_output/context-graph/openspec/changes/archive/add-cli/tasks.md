# Tasks — add-cli

- [x] argparse subcommands + `--json` for each.
- [x] Backend/embedder selection (default self-contained).
- [x] `sample.py` seeded graph.
- [x] `demo` end-to-end in-process.
- [x] Fold requirements into `specs/cli/spec.md`.
