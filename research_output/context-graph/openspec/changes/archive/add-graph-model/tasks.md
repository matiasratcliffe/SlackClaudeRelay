# Tasks — add-graph-model

- [x] Define `NodeType` enum and id helper.
- [x] Define `Node` dataclass with fields + defaults.
- [x] Define `SecondaryEdge` dataclass with fields + validity/version.
- [x] Implement `validate_new_node` (parent presence, root rules).
- [x] Implement `would_create_cycle` (ancestor walk).
- [x] Fold requirements into `specs/graph-model/spec.md`.
