# Tasks — add-embeddings-entrypoints

- [x] `EmbeddingProvider` interface + `cosine` helper.
- [x] `HashingEmbedder` (deterministic, offline, fixed-dim).
- [x] `find_entry_points` with relative cutoff + hybrid tag ranking + seed weights.
- [x] Fold requirements into `specs/embeddings-entrypoints/spec.md`.
