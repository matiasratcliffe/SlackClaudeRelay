# Tasks — iteration-1-hardening

- [x] `MemoryStore.save`/`load` (JSON, dataclass round-trip).
- [x] CLI `--db` load-on-start / save-after-mutation for the memory backend.
- [x] `ContextGraph.recompute_hub_summaries(threshold)`.
- [x] `tests/` pytest suite for model, embeddings/entry points, traversal, locking, ingest, export.
- [x] Run pytest green.
