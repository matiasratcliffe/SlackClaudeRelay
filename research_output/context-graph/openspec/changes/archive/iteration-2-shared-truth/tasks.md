# Tasks — iteration-2-shared-truth

- [x] `MountLink` model + store mount CRUD (memory incl. persistence, Neo4j Cypher).
- [x] `SecondaryEdge.similarity/tree_distance/rationale` + computation in `link()`.
- [x] `ContextGraph.mount()` with dedup/self-mount guards; `tree_distance()` helper.
- [x] Traversal mount hops; assembly renders edge distance.
- [x] Export `## Mounted here` section.
- [x] `structure.py` hardcoded taxonomy + `deploy_structure`; CLI `init --structure`, `mount`.
- [x] INTERFACE.md; PLANNING pi-section update (shared source of truth); spec updates.
- [x] Tests for structure, mounts, enrichment; suite green.
