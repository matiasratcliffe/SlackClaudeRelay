# Tasks — add-ingest-provenance

- [x] `ContextGraph` facade (store + embedder + locks).
- [x] `add_node` through lock + validate + auto-embed.
- [x] `link` with dedup/reinforce (CAS).
- [x] `Authority` enum + `supersede` (append-and-supersede).
- [x] Structural contradiction heuristic over functional verbs.
- [x] Fold requirements into `specs/ingest-provenance/spec.md`.
